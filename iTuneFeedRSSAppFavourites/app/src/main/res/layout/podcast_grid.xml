<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:id="@+id/linearLayoutPodcastGrid"
    android:paddingTop="@dimen/GridTopMargin">

    <FrameLayout
        android:layout_width="@dimen/GridWidth"
        android:layout_height="@dimen/GridHeight"
        android:id="@+id/frameLayoutImage"
        android:layout_gravity="center">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/imageViewTedview"
            android:layout_gravity="center"
            android:scaleType="fitXY"
            />

        <ImageView
            android:layout_width="@dimen/PlayButtonWidth"
            android:layout_height="@dimen/PlayButtonHeight"
            android:id="@+id/imageViewPlayButton"
            android:src="@drawable/play"
            android:scaleType="fitXY"
            android:layout_gravity="center" />

    </FrameLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:text="Medium Text"
        android:id="@+id/textViewPodcastTitle"
        android:gravity="center_horizontal" />
</LinearLayout>